#include <iostream>

int main() {
    
    //Explain in one sentence what is meant by "polymorphism"
    // using the same interface for multiple different types
    
    //Explain why polymorphism is a useful programming technique
    // reduces the amount of copy-code
    // defines common actions for different types of data
    // => reduces complexity/amount of crystallised knowledge
	
    //How is polymorphism supported by C++?
    // by inheritance(subtype polymorphism) or templates(parameter polymorphism)
    
    
    return 0;
}
