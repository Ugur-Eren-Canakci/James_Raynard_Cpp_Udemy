#include <iostream>

int main() {
    
    
    //• What is meant by "unbuffered input and output"?
    // the content is not kept in a memory before being flushed, i.e. input or output is done immediately without being stored in the memory
    
    // When is it useful?
    // HPC, hardware communications, ...
    
    //What do the read() and write() member functions do?
    // just ask chatgpt man
    
    
    //What does the gcount() member function do?
    // returns how many characters have been read
	return 0;
}
