#include <iostream>
#include <fstream>
int main() {
    
    //Briefly describe how files are represented in C++
    // by fstream objects
    
    //Explain what is meant by "sequential" access to a file
    // reading the files "bit-by-bit", i.e. processing every byte after one another
    
    //Why is it important to close a file after using it?
    // the program might be accessed by unwanted parties if left open(i.e. exposed) since a copy of it is now in the stack
    
    
	return 0;
}
