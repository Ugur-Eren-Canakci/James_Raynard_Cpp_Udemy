#include <iostream>

int main() {
    
    //Explain what the open() member of fstream does
    // called from an empty fstream object, opens the file, i.e. makes the fstream file work on the file
    
    //Name the member functions which can be used to check the state of a stream
    // .good(), .bad(), .fail(), .eof()
    
    //Name the member function which resets the state of the stream after an error
    // .clear()
	return 0;
}
