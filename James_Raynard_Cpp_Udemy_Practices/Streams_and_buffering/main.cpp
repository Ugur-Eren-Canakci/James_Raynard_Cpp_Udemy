#include <iostream>

int main() {
    
    
    //What effect does the flush manipulator have?
    // it force-loads an output stream with what's kept in the buffer to already be put on the output stream
    
    //Give an example of a situation where it could be useful
    // while keeping log files, especially in an unexpected crash/abort situation
    
    // What effect does the endl manipulator have?
    // makes the console/file marker go to the next line
    
    
    
    return 0;
}
