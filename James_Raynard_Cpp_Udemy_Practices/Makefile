.PHONY: clean All

All:
	@echo "----------Building project:[ Swap - Debug ]----------"
	@cd "Swap" && "$(MAKE)" -f  "Swap.mk"
clean:
	@echo "----------Cleaning project:[ Swap - Debug ]----------"
	@cd "Swap" && "$(MAKE)" -f  "Swap.mk" clean
