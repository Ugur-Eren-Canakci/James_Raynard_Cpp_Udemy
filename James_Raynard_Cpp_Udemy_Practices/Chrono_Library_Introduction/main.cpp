#include <iostream>

int main() {
    
    //Briefly describe the three main concepts that the std::chrono library is based on
    // clocks have a starting point and a tick rate
    // time point is a duration of time that has passed since the epoch of a specific clock
    // duration is the number of ticks between time units?
    
	return 0;
}
