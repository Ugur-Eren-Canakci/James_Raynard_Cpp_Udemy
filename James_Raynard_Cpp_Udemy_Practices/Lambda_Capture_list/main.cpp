#include <iostream>

int main() {
    
    // What access to other variables does a lambda expression have by default?
    // can access global vars and static vars(has to be in the same scope)
    
    
	return 0;
}
