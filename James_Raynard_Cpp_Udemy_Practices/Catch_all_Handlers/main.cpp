#include <iostream>

int main() {
    
    //• What is a catch all handler?
    // a catch block that takes in all possible thrown objects
    
    //Give an example of when a catch-all handler could be useful
    // when you're using external source code, you might not have an idea what kind of exceptions are thrown
    // but you still have to handle the exception case. Hence you have to catch objects out of your knowledge scope
    
    
    return 0;
}
